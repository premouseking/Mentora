---
description: 代码注释生成规范（中文、工程化、含约束）
alwaysApply: true
---

# 代码注释规范

AI 生成或修改代码时，注释须遵循本规范。注释语言为**简体中文**（用户可见的错误文案除外）。

## 何时写注释

- **写**：模块边界、安全/权限约束、非显而易见的业务规则、跨进程/跨层协议、与架构文档的对应关系
- **不写**：类型与命名已自解释的 getter/setter、逐行复述代码、仅为满足「要有注释」的废话

## 模块级注释（文件头 / 注册表 / 边界模块）

使用 JSDoc 块，按固定结构组织（按需省略空段，但顺序不变）：

```typescript
/**
 * <一句话职责>
 *
 * 约定：<命名、协议、数据形态等团队约定>
 *
 * 约束：
 * - <调用方必须遵守的规则>
 * - <禁止的行为>
 *
 * @see docs/architecture/... §x.x   // 有架构依据时引用
 * @module path/to/module   // 共享契约、边界模块可选
 */
```

## 函数 / 类型 / 常量

- 公共导出符号：仅在含**非显而易见约束**时写一行 JSDoc（优先写「约束：…」）
- 安全敏感常量（allowlist、TTL、大小上限）：说明约束来源即可
- 能力域分组：结构已清晰时不加分区注释

## 行内注释

- 只解释**为什么**（权衡、平台差异、安全原因），不解释**做了什么**
- 多步骤流程用编号步骤（`// 1. …`），一步一行
- 禁止 `// TODO: fix later` 式占位；若必须留 TODO，写清阻塞条件与预期行为

## 禁止

- 英文注释（协议字面量、第三方 API 字段名等专有名词除外）
- 复述标识符：`// 获取用户信息` 放在 `getUserInfo()` 上
- 无约束的泛泛描述：`// 处理请求`、`// 初始化`
- 注释与实现/类型不一致；改代码时同步改注释

## 正反例

```typescript
// ❌ 英文 + 复述
/** Returns the user status */
function getStatus() { ... }

// ❌ 无约束的空话
// 处理 API 请求

// ✅ 工程化
/**
 * 带认证的 API 桥接。
 *
 * 约束：非开放代理；path 须匹配 allowlist，body 有大小上限（§5.1）。
 */
```
