---
description: Git / GitHub 协作范式（AI 与开发者）
alwaysApply: true
---

# Git / GitHub 操作范式（强制）

人类协作者见 [CONTRIBUTING.md](../../CONTRIBUTING.md)。Shell 拦截见 `shell-file-safety.mdc` 与 `.cursor/hooks/`。

## AI 禁止直接执行的 Git 写操作

AI **不得**通过 Shell 或等价途径直接执行以下命令（含变体、链式、`git -C` 前缀）：

- `git add`、`git commit`、`git push`、`git pull`
- `git merge`、`git rebase`、`git cherry-pick`
- `git reset`、`git checkout`、`git restore`、`git switch`
- `git stash`（含 `push` / `pop` / `apply` / `drop`）

即使用户未启用 hook，规则层面同样禁止 AI 擅自提交、推送或改写 Git 历史。

**AI 允许**的只读 Git 操作：`status`、`diff`、`log`、`show`、`branch -v`、`remote -v`、`stash list`、`tag -l`、`describe`、`blame`、`ls-files`。

## AI 标准协作方式

1. 先检查仓库状态：当前分支、未提交改动、远端跟踪、最近提交风格。
2. 基于 `git diff` 起草 **commit message**（优先写清「为什么」，不要只罗列「改了什么」）。
3. 向开发者输出**可复制、最小、安全**的命令序列，并说明分支流向与暂存文件范围。
4. 由**开发者**在本地终端执行写操作；AI 不替用户点「提交」。
5. 开发者执行后若需继续（push、开 PR、合并），把终端输出或 `git status` 交回，AI 再接管下一步判断。

### 推荐输出格式

```text
## 建议提交

<type>: <简短中文说明>

<一段具体的中文改动说明>

## 建议命令（请在本机终端执行）

git add <明确文件列表>
git commit -m "$(cat <<'EOF'
<type>: <简短中文说明>

<一段具体的中文改动说明>
EOF
)"
git status
```

PowerShell 环境可改用 here-string 或多行 `-m`；关键是标题与正文之间**空一行**。

### 确认话术（push / merge / 开 PR 前）

涉及 push、merge、rebase 或创建 PR 时，先说明拟执行步骤与分支流向，**等开发者确认**后再给出命令；不要默认 AI 已代劳。

```text
准备执行：
1. 提交当前改动到 feat/<task-id>-<description>
2. push 到 origin/feat/<task-id>-<description>
3. 创建 PR 合入 main

建议 commit message:
fix: ...

是否按此方案在本机执行？确认后我将给出完整命令。
```

### 开发者明确要求「只给命令」时

优先输出**可直接复制**的完整命令序列，逐步标注分支流向；仍不替开发者执行写操作。

### Hook 拦截 Git 写操作时

环境 hook 禁止 AI 执行 Git 写操作时，AI 仍负责：判断 merge/commit 状态、处理冲突思路、起草 message；并给出**当前最小可执行命令**供开发者粘贴。开发者执行后再交回结果，AI 继续下一步。

## 仓库分支约定

- 功能开发：`feat/<task-id>-<description>`
- 缺陷修复：`fix/<task-id>-<description>`
- 文档：`docs/<task-id>-<description>`
- 除非开发者**明确要求**新建或切换分支，AI 默认沿用当前分支，不擅自 `checkout` 到其他分支。
- 功能开发中不要混入无关重构；优先小而可验证的提交。

## Commit 信息格式

```text
<type>: <简短中文说明>

<一段具体的中文改动说明>
```

标题与正文之间**必须空一行**。

类型：`feat` `fix` `refactor` `docs` `test` `style` `perf` `chore` `ci` `revert`

## 冲突处理原则

- 禁止无脑 `-X ours` / `-X theirs`。
- 必须逐文件 review 冲突来源。
- 默认以当前 feature 的业务目标为主，同时吸收远端必须保留的修复与契约变更。
- 冲突未判断清楚时先停下来向开发者说明，再继续。

## GitHub 平台操作

- 创建或更新 PR、查看 checks、读取 issue：优先使用 **GitHub MCP** 或 **`gh` CLI**（只读与平台 API 调用不受「禁止 Git 写操作」约束）。
- 纯本地分支同步仍按上文「只给命令、开发者执行」流程处理。

## 汇报要求

涉及 Git 协作时，优先说明：

- 当前分支名
- 本地改动是否已提交、建议或已用的 commit message
- 建议暂存的文件列表（避免 `git add .` 误伤）
- 下一步建议（push / PR / 合并）及需开发者确认的点

## 记忆写回

开发者明确表达的稳定 Git 偏好（分支流、确认方式、提交信息习惯）应写回本文件对应小节，避免重复确认。
